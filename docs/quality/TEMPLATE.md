# 代码质量控制审核报告（模板）

> **模板版本**：v1（2026-06-27 建立）
> **使用方式**：每次定期审核时，复制本文件到 `docs/quality/logs/`，按 `YYYY-MM-DD-audit-NN.md` 命名（NN 为当月审核序号），逐节填写。
> **填写原则**：① 论据带 `文件:行号`，可点击可追溯；② 区分"真问题"与"被证伪的疑虑"，后者也要记录以体现严谨；③ 行动项必须进入第 5 节整改追踪表，闭环管理。

---

## 元信息

| 项 | 内容 |
|---|---|
| 审核编号 | `<YYYY-audit-NN>` |
| 审核日期 | `<YYYY-MM-DD>` |
| 代码版本（commit） | `<git short SHA>` |
| 审核范围 | `<全量 / 指定模块，列出文件或目录>` |
| 审核方式 | `<人工精读 / 工具辅助 / 子代理并行，简述方法>` |
| 上次审核 | `<上一份 log 文件名，或「首次」>` |

---

## 1. 📊 整体健康度评分

| 维度 | 评分(/10) | 较上次 | 一句话总评 |
|------|:---:|:---:|------|
| 架构与目录结构 | | `↑/↓/→` | |
| 模块拆分与解耦 | | | |
| 代码质量与可维护性 | | | |
| 技术栈最佳实践 | | | |

**总评**：`<2-3 句：项目当前整体状态、最突出的优点与短板、健康度趋势>`

---

## 2. 🚨 核心问题清单（High Priority）

> 只列最致命的 2–3 个（严重耦合 / 性能隐患 / 内存泄漏 / 数据安全 / 正确性 bug）。每条给唯一编号，便于在整改追踪表引用。

### 🔴 P0-1：`<标题>`
`<现象 + 影响 + 关键 文件:行号>`

### 🔴 P0-2：`<标题>`
`<...>`

---

## 3. 🔍 逐项审计与重构建议

> 每个问题三段式：**Where（文件:行号）→ Why（为什么是问题）→ How（Before / After 示范代码）**。

### 3.1　`<问题标题>`
- **文件**：`<path:line>`
- **Why**：`<根因与风险>`

**Before**
```ts
// 现状代码
```
**After**
```ts
// 重构后
```

### 3.x　其余记录项（中低优先级）

| 位置 | 问题 | 建议 |
|------|------|------|
| | | |

> **被证伪的疑虑（澄清区）**：`<审核中曾怀疑、但取证后确认无问题的点，注明证据。例：疑似内存泄漏 → 实测 useEffect 清理成对（path:line），无泄漏。>`

> **值得保持的亮点**：`<已经做对、不应在重构中破坏的设计，注明 文件:行号。>`

---

## 4. 🛠️ 长期维护与优化行动指南

> 按"低风险高收益"排序，分梯队。每项可独立提交、互不阻塞。

**第一梯队（立即做，低风险）**
1. `<行动项>`

**第二梯队（1–2 迭代，中风险）**
1. `<行动项>`

**第三梯队（需先出设计稿）**
1. `<行动项>`

**贯穿纪律**
- `<例：单文件 > 500 行触发拆分评审；关键注释固化为单测。>`

---

## 5. ✅ 整改追踪

> 本次审核产出的所有行动项进入此表。后续审核**先回填上一份的本表**，再开新报告——确保问题闭环，不重复发现、不无限拖延。

| 编号 | 行动项 | 关联问题 | 优先级 | 状态 | 完成 commit | 备注 |
|---|---|---|:---:|:---:|---|---|
| A-1 | `<行动项>` | P0-1 | 高 | ☐ 待处理 / ◐ 进行中 / ☑ 已完成 / ⊘ 已搁置 | `<SHA>` | |

**状态图例**：☐ 待处理　◐ 进行中　☑ 已完成　⊘ 已搁置（注明原因）
