# 代码质量控制审核报告 · 2026-audit-02

> 依据 `docs/quality/TEMPLATE.md` v1 填写。第二轮全量质量审核——验证 audit-01 重构成果（A-5~A-14）+ 审计其后新增功能（role-chat 增强 / m2-cards 7 项 / 品牌 DemonForge）+ 复评整体健康度。

---

## 元信息

| 项 | 内容 |
|---|---|
| 审核编号 | 2026-audit-02 |
| 审核日期 | 2026-06-28 |
| 代码版本（commit） | `527c71c`（branch main） |
| 审核范围 | 全量复审。重点：① 重构产物（store/slices、persistence/bootstrap、sse、cleanScheduler/dequeue/circuitBreaker、processKiller）；② audit-01 后新增功能（role-chat、m2-cards、品牌）；③ 全局 SSE 采用彻底性与数据目录一致性取证 |
| 审核方式 | 主审人工精读（重构核心 + 后端创作/聊天路由 + 新功能 service/组件）+ 结构化取证（行数排名、`getReader`/`TextDecoder` 手写 SSE 扫描、`settings.json`/`dataDir` 读取点扫描）+ vitest 回归（**11 文件 55 例全绿**）。注：2 个并行子代理（node-test / 新功能审计）因 API 429 限流零产出（与 audit-01 同），改由主审亲自全覆盖 |
| 上次审核 | `2026-06-27-audit-01.md` |
| 复审走查 | **2026-06-28 第二次走查**（HEAD `3c63d96`）：并行开发后重新审计——验证 B-1/B-2 修复真实性 + 审计全新 monopoly 模块 + 回填 B-3~B-11 + 新发现 C-1/C-2，详见 **第 6 节** |
| 修复走查 | **2026-06-28 第三次走查·修复**（待提交）：按复审优先级顺序修复 C-1 / C-2 / B-3 / B-4 / B-6 / B-9 / B-10 / B-11 共 8 项，回归全绿（后端 tsc 0 + 前端 build + vitest 55 + 改动文件 eslint 0），详见 **第 7 节** |

---

## 0. 上轮（audit-01）整改回填

> 本轮先确认上一份追踪表闭环，再开新报告（TEMPLATE 第 5 节纪律）。

audit-01 的 14 项行动项**已全部收口**：

| 区段 | 状态 |
|---|---|
| A-1~A-4（删死文件 / UTC bug / vitest 地基 / 首批单测） | ☑ 完成（`d58883c`） |
| A-5（parseSSE 抽象） | ☑ 完成（`e8c0557`） |
| A-6（CleanScheduler 类化） | ☑ 完成（`82e1728` 等 5 提交） |
| A-7（appStore 切片化） | ☑ 完成（`0222a0d`/`3256bea`） |
| A-8（node-test/settings 拆分） | ☑ 完成（`645c495` 等） |
| A-9（服务层 mock 收口） | ☑ 完成（`8bb288c`） |
| A-10（主题色收敛） | ☑ 完成（`fbf6a48`） |
| A-11（生图 client 抽接口） | ⊘ won't-fix（协议本质不同，过度抽象） |
| A-12（fetch 猴补丁 → apiFetch） | ⊘ deferred（涉 42 处生产路径，风险/工程量不匹配） |
| A-13（M1 懒加载） | ☑ 完成（`6183645`） |
| A-14（杀进程抽 processKiller） | ☑ 完成（`6183645`） |

**本轮复核结论**：A-5~A-10/A-13/A-14 经精读确认**真实落地、行为不退化**（详见第 3 节亮点与证伪区）。**但发现 A-7 只搬移、未消除 audit-01 P0-1 的核心子问题（手写脏检查变更放大器）**，且 audit-01 对 P0-2（SSE 重复）的扫描遗漏了组件层一处——两者转为本轮新行动项（B-3 / B-4），不视为上轮未闭环。

---

## 1. 📊 整体健康度评分

| 维度 | 评分(/10) | 较上次 | 一句话总评 |
|------|:---:|:---:|------|
| 架构与目录结构 | 7.5 | ↑（6.5） | store 按域分层、持久化/引导抽离、测试地基（11 文件）、SSE 统一——目录结构显著规整，地基更扎实。 |
| 模块拆分与解耦 | 6.5 | ↑（4.5） | 全项目最弱项大幅改善：appStore 1152→55、node-test 2355→445、CleanScheduler 闭包→类。残留三个胖文件（useInferenceSession 780 / settings 1428 / creation 731）。 |
| 代码质量与可维护性 | 6.5 | ↑（6.0） | 测试 0→55 例、UTC bug 修复、lint 清零——硬质量继续走高；但本轮在**新功能**里查出 chat.ts 确定性生产 bug、role-chat 自动循环资源/闭包问题，抵消部分增益。 |
| 技术栈最佳实践 | 7.5 | ↑（7.0） | audit-01 点名"未利用"的模式——parseSSE async generator、zustand slices、custom hooks、vitest——本轮已大量兑现。 |

**总评**：**重构线大获成功，四维全面向好**——audit-01 开出的行动项不仅做完，且经精读确认实现干净（持久化竞态防护、熔断器解耦、parseSSE 边界都是教科书级）。健康度趋势明确向上。**但审计重心已从"重构债"转移到"新功能债"**：与重构并行推进的 role-chat 增强、m2-cards 未受同等质量纪律约束，引入了数据目录不一致（打包版 role-chat 失效）、资源生命周期缺口（自动循环卸载后续跑）、React 闭包陷阱（自动循环用陈旧历史）等 user-facing 问题。结论：**重构可收官，质量纪律需向新功能扩面**。

---

## 2. 🚨 核心问题清单（High Priority）

### 🔴 P0-1：role-chat 本地模式在 Electron 打包版必然失效（后端读错配置路径）
`server/src/routes/chat.ts:45` 用 `new URL('../data/settings.json', import.meta.url).pathname` 直接读**源码旁**配置，是全后端**唯一**绕过统一数据目录抽象 `getAppDataDir()` 的地方（对比 `routes/settings.ts:7`、`routes/importSession.ts:6` 均走 `getAppDataDir()`，优先 `NOVELHELPER_DATA_DIR`）。开发环境两路径碰巧都指向 `server/src/data/` 故可用；**打包版数据目录为 `~/.novelhelper/`，chat.ts 仍读打包目录内的 data → `settings.providers` 为空 → `provider` 必然 `find` 不到 → 返回 404「节点不存在」**。即使用户在打包版手动配置了节点（写入 `~/.novelhelper/settings.json`），本地角色对话仍读不到。影响：role-chat 本地模式（核心功能之一）在生产形态完全不可用。

### 🔴 P0-2：role-chat 自动循环——流不可中断 + 卸载后继续运行 + 历史快照陈旧
`pages/role-chat/index.tsx` 的自动循环存在三个叠加问题：
- **流不可中断**：`respondLocal:118` 调 `sendLocalRoleMessage(...)` **未传 `signal`**，而 `services/real/roleChat.ts:76` 明明支持 `signal?` 形参。点「停止循环」只能在两次响应之间通过 `abortRef`（布尔）生效，**无法打断正在进行的 SSE 流**。
- **卸载无清理**：整个页面**没有 `useEffect` 卸载钩子**去置 `abortRef.current=true`。用户在循环中切走页面 → `runAgentLoop` 的 `while (!abortRef.current)`（`:325`）继续在后台跑 → 持续向上游 LLM 发请求（烧 token）+ 对**已卸载组件** `setMessages`/`setParticipants`（React 警告）。
- **历史快照陈旧（疑似正确性 bug）**：`runAgentLoop` 是长 async 循环，经 `respondLocal:121` 引用的 `messages` 是**启动循环那一刻的闭包快照**（非函数式读取）。循环期间各 Agent 的 LLM 请求只携带启动时的对话历史，**互相看不到对方在循环中的新发言**——UI 上消息正常累积（`setMessages` 用函数式更新），但模型上下文停滞，自动循环退化为"各 Agent 对初始消息各说各话"，削弱"多角色交流"的核心价值。机理依据 React 闭包语义可确认；用户感知程度建议实测验证。

---

## 3. 🔍 逐项审计与重构建议

### 3.1　chat.ts 数据目录读取（对应 P0-1）
- **文件**：`server/src/routes/chat.ts:45-58`
- **Why**：硬编码 `new URL('../data/settings.json')` 与全项目 `getAppDataDir()` 单一真相源不一致；生产数据目录 `~/.novelhelper/` 下读不到 providers。

**Before**
```ts
const settingsPath = new URL('../data/settings.json', import.meta.url).pathname
const settingsText = readFileSync(settingsPath, 'utf-8')
const settings = JSON.parse(settingsText)
provider = settings.providers?.find((p: any) => p.id === nodeId)
```
**After**
```ts
import { readSettings } from './settings' // 已有：走 getAppDataDir() + .bak 回退
const providers = (readSettings().providers ?? []) as ProviderNode[]
const provider = providers.find((p) => p.id === nodeId)
```
> 顺带消除 `card: any`(`:31`)/`provider: any`(`:46`) 两处后端裸 `any`（B-7）。

### 3.2　role-chat 自动循环生命周期（对应 P0-2）
- **文件**：`pages/role-chat/index.tsx:55, 103-168, 313-369, 372-413`
- **Why**：见 P0-2。`abortRef` 用布尔而非 `AbortController`，流不可中断；无卸载清理；闭包捕获陈旧 `messages`。

**After（要点）**：
1. 引入真 `AbortController`（替代布尔 `abortRef`），`respondLocal` 透传 `signal` 给 `sendLocalRoleMessage`。
2. 加 `useEffect(() => () => { acRef.current?.abort(); abortRef.current = true }, [])` 卸载清理。
3. 用 `messagesRef`（`useRef` 镜像 + 每次 `setMessages` 同步）或在 `respondLocal` 内函数式读取最新 messages，喂给后端的 `conversationHistory` 取 `messagesRef.current`。

### 3.3　持久化脏检查未声明式化（承接 audit-01 P0-1，A-7 残留）
- **文件**：`store/persistence.ts:277-299`（业务 12 项 `===`）、`:302-324`（设置 20 项 `===`）；与 `businessPayload:19-32`、`settingsPayload:158-179` 三处需手工同步
- **Why**：audit-01 P0-1 的核心担忧是"手写脏检查 = OCP 违反 + 变更放大器，新增字段易漏比较导致改了不落库"。A-7 把上帝 Store 拆成 6 切片（主目标达成），但脏检查只是从 `appStore.ts` **整体搬到** `persistence.ts`，**未改成声明式**——设置项还从 13 增到 20，脆弱面更大。

**After（要点）**：以 payload 的键集为单一真相，自动生成比较，加字段只改 payload 一处：
```ts
const SETTINGS_KEYS = Object.keys(settingsPayload(useAppStore.getState())) as (keyof AppState)[]
useAppStore.subscribe((s, prev) => {
  if (!storeReady) return
  if (SETTINGS_KEYS.every((k) => s[k] === prev[k])) return
  // ...debounce POST
})
```

### 3.4　settings 组件内手写 SSE 解析（audit-01 P0-2 漏查的残留点）
- **文件**：`pages/settings/index.tsx:274-296`（`startRealTest`）
- **Why**：audit-01 只扫了 `services/real/*.ts`，遗漏组件层。此处 `getReader + TextDecoder + buffer.split('\n\n') + 逐行 data: + JSON.parse` 是完整的手写帧解析，且仅消费 `delta`/`text`、无特殊需求，**可直接换用 `services/sse.ts::parseSSE`**。（同文件 `:96` 的 `testProvider` 只读流探活、不解析事件，可不动。）

**After**：`for await (const { event, data } of parseSSE(res.body)) { if (event==='delta'||event==='done') acc += (data as any).delta ?? (data as any).text ?? '' }`

### 3.5　useInferenceSession 拆分未透（A-8 残留胖 hook）
- **文件**：`pages/node-test/hooks/useInferenceSession.ts`（780 行）
- **Why**：A-8 把 2355 行胖组件拆到 index(445)+ hook，方向正确且被 `index.test.tsx` 锁住；但 hook 本身成了 780 行新胖块：① 对比模式 `handleGenerateSide:404-567` 与 `retryCompareMessage:274-357` 的 `streamChat` 回调几乎逐字重复，`side === 'left' ? A : B` 三元出现 30+ 次；② 对比模式与单栏模式混在一个 hook；③ 对比模式 `acRefLeft/Right` 无组件卸载清理（单栏经 sendInSession/引擎管理，无此问题）。
- **建议**：把对比模式整体抽 `useCompareSession`，左右两侧用 `side` 参数化的 `{setter, acRef, setPhase, setDebugInfo}` 字典消除 if/else 重复；补卸载 `useEffect` abort 左右 acRef。非阻塞，可独立提交。

### 3.6　其余记录项（中低优先级）

| 位置 | 问题 | 建议 |
|------|------|------|
| `server/src/routes/creation.ts`（731 行 / 11 端点） | 单文件承载 arch/blueprint/draft/finalize/consistency/simulate/extract/generate-card×2/card-image-prompts 全部创作端点 | 按领域拆 `creation.origin.ts`（arch/blueprint）/ `creation.generate.ts`（draft/finalize/consistency）/ `creation.m2.ts`（extract/card），共用 `streamChat` helper |
| `store/bootstrap.ts:69` | `cleanNodeOverrides` 本地类型注解写 `batchSize`，实际字段是 `batchChars`（`types.ts:170`）；因整体透传未致运行时 bug，但误导读者 | 改 `batchChars` 对齐 |
| `pages/m2-cards/CardEditorModal.tsx:64`、`SceneSettingModal.tsx:25`、`ImageBatchModal.tsx:69` | 三个新弹窗均靠 `handleClose` 手动 abort，无 `useEffect` 卸载清理；父组件非 onCancel 路径卸载时在途流不中止 | 加统一卸载 `useEffect(() => () => acRef.current?.abort(), [])`（实际都走 onCancel，风险低） |
| `pages/role-chat/components/SceneSettingModal.tsx:5`、`role-chat/index.tsx:28` | 直接 `import from services/real/...`，绕过 `services/api.ts` 服务层抽象 | 统一经 api.ts re-export（保持 mock/real 可切换） |
| `server/src/utils/sseHelper.ts:14` | `Access-Control-Allow-Origin` 硬编码 `http://localhost:5173`，与主 CORS（`index.ts:35-43` 放行 `'null'`/file://）不一致 | 回显请求 `Origin`（在白名单内）或与 `ALLOWED_ORIGINS` 对齐——当前 Electron 下未阻断（疑似 webSecurity 豁免），但改端口/收紧安全即脆断，建议主动对齐 |

> **被证伪的疑虑（澄清区）**
> - **疑似 P0-2 残留：`llm.ts:154/261` 两处手写 SSE → 实为合理例外**。注释（`:108-111`）说明充分：清理流需累积原始字节 `rawChunks` 供失败时 Debug `responseBody` 诊断，且 `streamBatch` 要按 `CHAPTER_SEP` 增量拆分多章——这两点 `parseSSE`（纯 `{event,data}`）不覆盖。非遗漏，是 A-5 的明确边界。
> - **疑似新功能 SSE 退化 → 否**。`services/real/roleChat.ts:100`、`cardGen.ts:105`、`chat.ts`（前端）新流式代码**全部采用 `parseSSE`**（grep 确认 12 文件引用），无一手写——重构成果被新代码正确继承。
> - **疑似 CleanScheduler 类化引入正确性退化 → 否**。`cleanScheduler.ts` 熔断/重试预算/节点避让/热更新逻辑逐字保留，`NodeCircuitBreaker`（状态机）与 `dequeueBatch`（纯函数）解耦且各有单测；`cleanScheduler.test.ts`（285 行 characterization 网）+ 55 例全绿护住行为。
> - **疑似 role-chat 导出/重置泄漏 → 无**。`handleExport/handleExportTxt`（`:249-305`）`URL.createObjectURL` + `revokeObjectURL` 成对；`handleReset`（`:240`）清 `opcodeSessionsRef`。

> **值得保持的亮点（重构勿破坏）**
> - **持久化竞态防护**（`persistence.ts`）：`enqueueWrite` 串行链（防 POST/DELETE 乱序复活，`:36-41`）、`keepalive`、`storeReady` 门控、`pushStoreNowChecked` 显式区分"吞错 fire-and-forget"与"抛错供 await"（`:94-153`）——竞态/关窗丢写的踩坑全部固化在代码里。
> - **熔断器/出队纯化**（`circuitBreaker.ts` / `dequeue.ts`）：副作用与判定解耦，纯函数可独立单测。
> - **parseSSE 边界清晰**（`sse.ts`）：坏帧兜底为原始字符串不炸断流、`finally releaseLock`、传输层与业务分发分离。
> - **设置原子写入**（`settings.ts:57-72`）：`.tmp` → `rename` + `.bak` 回退，根治断电截断致配置静默丢失。
> - **后端断连中止合规**（`sseHelper.ts:21` + `creation.ts:405-443` 等）：监听 `reply.raw` 的 `close` → `ac.abort()`，注释明确"必须监听响应而非 req.raw"。
> - **新功能复用而非复造**：`CardEditorModal` 复用 `DebugInfoPanel`（`:9`）；`cardGen.ts` 逐字段 `typeof` 防御性解析（`:57-66`）。

---

## 4. 🛠️ 长期维护与优化行动指南

**第一梯队（立即做，低风险）**
1. **修 P0-1**：chat.ts 改用 `readSettings()`（B-1）。单文件、机械替换、消除生产 bug。建议补一条 role-chat 本地模式的端到端冒烟（打包版手动配节点 → 发消息）。
2. settings 组件手写 SSE → `parseSSE`（B-4）。一处替换，去重。
3. bootstrap.ts `batchSize`→`batchChars` 类型注解修正（B-6）。

**第二梯队（1–2 迭代，中风险）**
1. **修 P0-2**：role-chat 自动循环引入 `AbortController` + 卸载清理 + `messagesRef` 消陈旧（B-2）。改动集中在单文件，但涉及循环/并发语义，需配合实测（手动停止打断在途流、切走页面停循环、循环中 Agent 能看到彼此新消息）。
2. 持久化脏检查声明式化（B-3）：以 payload 键集驱动比较，根除"加字段漏比较"。有 `persistence.test.ts` 护网。

**第三梯队（需先出设计稿）**
1. useInferenceSession 抽 `useCompareSession` + side 参数化消重（B-5）。
2. creation.ts 按领域拆三文件（B-8）。

**贯穿纪律**
- **新功能纳入与重构同等的质量门**：本轮所有新发现问题都在"重构未覆盖的新功能"里（role-chat / m2-cards / chat 路由）。建议新增功能 PR 自检三项：① SSE 流式是否复用 `parseSSE`；② `AbortController` 是否在卸载/停止时成对 abort；③ 后端读配置是否走 `getAppDataDir()`。
- 单文件 > 500 行触发拆分评审（当前越线：settings 1428 / Step3Clean 1226 / ImmersiveReader 1081 / useInferenceSession 780 / Step4Review 769 / m2-cards 723 / creation 731 / image-helper 705 / role-chat 695）。
- 关键生命周期注释（流不可中断 / 数据目录单一真相源）固化为测试或自检清单。

---

## 5. ✅ 整改追踪

> 本轮新行动项以 **B-** 前缀编号（A- 为 audit-01）。

> **状态列已于 2026-06-28 第二次走查回填**（见第 6 节）。`完成 commit` 指并行开发实际落地的提交。

| 编号 | 行动项 | 关联问题 | 优先级 | 状态 | 完成 commit | 备注 |
|---|---|---|:---:|:---:|---|---|
| B-1 | chat.ts 改用 `readSettings()`（走 getAppDataDir），修 role-chat 打包版读不到 providers | P0-1 / 3.1 | 高 | ☑ 已完成（换方案） | `3c63d96` | role-chat 重构**直接废弃删除 chat.ts**，前端 roleChatEngine 直传节点给 `/api/llm/chat`，后端零状态——路径 bug 从根消除。复审确认真修复，优于原计划 |
| B-2 | role-chat 自动循环：AbortController 替布尔 + 卸载 useEffect 清理 + messagesRef 消陈旧 | P0-2 / 3.2 | 高 | ◐ 部分（2/3） | `3c63d96` | 子问题1（流可中断）✅、子问题3（闭包陈旧→单一数据源纯函数派生）✅ 真修复；**子问题2（卸载清理）未落地**——index.tsx 仍无 useEffect，切走路由后循环后台续跑烧 token。转 **C-2** |
| B-3 | 持久化脏检查声明式化（payload 键集驱动比较） | 3.3（承接 audit-01 P0-1） | 中 | ☑ 已完成 | 待提交 | `registerPersisters` 内 `BUSINESS_KEYS`/`SETTINGS_KEYS = Object.keys(payload(getState))`，两处订阅改 `KEYS.every(k=>s[k]===prev[k])`；加字段只改 payload 一处，C-1 类漏写从根杜绝 |
| B-4 | settings/index.tsx:274-296 手写 SSE → parseSSE | 3.4（audit-01 P0-2 残留） | 中 | ☑ 已完成 | 待提交 | startRealTest 改 `for await parseSSE`，按 event 分发（delta 累积 / done 覆盖）；顺带理顺原 done 重复累积致结果显示两遍 |
| B-5 | useInferenceSession 抽 useCompareSession + side 参数化消重 + 对比 acRef 卸载清理 | 3.5（A-8 残留） | 中 | ☐ 待处理 | | 仍 780 行，hooks 目录无 useCompareSession |
| B-6 | bootstrap.ts:69 cleanNodeOverrides 类型注解 batchSize→batchChars | 3.6 | 低 | ☑ 已完成 | 待提交 | :66 inline 类型注解改 `batchChars`，对齐 types.ts:182 |
| B-7 | chat.ts card/provider 两处 any 收紧为 interface | 3.6 | 低 | ⊘ 作废 | `3c63d96` | chat.ts 已随 role-chat 重构删除，问题随文件消失 |
| B-8 | creation.ts(731) 按领域拆三文件 | 3.6 | 低 | ☐ 待处理 | | 仍 731 行单文件，routes/ 无拆分 |
| B-9 | 三个新弹窗补卸载 useEffect abort（统一模式） | 3.6 | 低 | ☑ 已完成 | 待提交 | CardEditorModal/ImageBatchModal/SceneSettingModal 各加 `useEffect(()=>()=>acRef.current?.abort(),[])` |
| B-10 | role-chat/SceneSettingModal 绕过 api.ts 的 import 收口 | 3.6 | 低 | ☑ 已完成 | 待提交 | SceneSettingModal 与 roleChatEngine 的 `streamChat`/`ChatMessage` 改经 `services/api` re-export |
| B-11 | sseHelper.ts 硬编码 ACAO 与 ALLOWED_ORIGINS 对齐 | 3.6 | 低 | ☑ 已完成 | 待提交 | 经 `reply.request.headers.origin` 回显白名单内 Origin（含 file:// 的 `null`），单文件零调用点改动 |
| **C-1** | roleChatAutoConfig 加入 settingsPayload + 两处订阅比较（修持久化丢失） | 复审 6.4（B-3 实例） | **中高** | ☑ 已完成 | 待提交 | 加入 settingsPayload；订阅比较随 B-3 声明式化自动覆盖。后端 `/api/settings` merge 透传已证闭环；护网 persistence.test 期望同步更新（20→21 键） |
| **C-2** | role-chat index.tsx 补卸载 useEffect（abort 全 participant + 置 abortRef） | 复审 6.2（B-2 残留） | 中 | ☑ 已完成 | 待提交 | 加卸载 `useEffect`：置 `abortRef.current=true` + 读 store 最新 participants 逐个 `cancelParticipant`（不依赖闭包快照） |

**状态图例**：☐ 待处理　◐ 进行中／部分　☑ 已完成　⊘ 已搁置（注明原因）

---

## 6. 🔄 复审回填（2026-06-28 第二次走查）

> 本节为 audit-02 报告提交（`efacca6`）后的**第二次走查**。期间并行开发推进了两条线：**role-chat 模块整体重构**（`3938f91`+`3c63d96`，直接触及 B-1/B-2）与**全新 monopoly 游戏模块**（P0–P6 共 7 提交 `835d8de`→`616a220`，原报告未覆盖）。复审独立验证 HANDOFF 的修复声明，并审计新代码——不轻信自我声明。

### 6.0　方法与基线
- **范围**：`527c71c`（原审计版本）→ `3c63d96`（当前 HEAD）的全部增量。
- **方法**：主审亲自全覆盖（2 个并行子代理再次因 API 429 零产出，同前两轮）。回归基线：`server tsc --noEmit` **0** + `frontend build`（tsc -b + vite）成功 + `vitest run` **11 文件 55 例全绿**（与上轮持平、无回退）。
- **取证重点**：chat.ts 删除核实、roleChatEngine 数据流、role-chat 生命周期、persistence/bootstrap 持久化链、monopoly 纯度/资源/类型。

### 6.1　B-1 ☑ 真修复（换方案，优于原计划）
原计划是 chat.ts 改用 `readSettings()`；实际重构走得更彻底——**整端点废弃删除**（`server/src/routes/chat.ts` 与 `services/real/roleChat.ts` 均已不存在）。前端 `roleChatEngine.ts:136` 由 `streamChat` 直传选定节点的 `baseURL/apiKey/model` 给通用 `/api/llm/chat`，后端零状态、不读任何 settings 文件（`services/real/chat.ts:55`）。**绕过 `getAppDataDir()` 的路径 bug 从根消除**，打包版 role-chat 不再受影响。复审结论：真修复。

### 6.2　B-2 ◐ 仅 2/3 修复（子问题 2 未落地 → C-2）
- **子问题 1（流不可中断）✅**：每参与者一个真 `AbortController`（`roleChatEngine.ts:110`），`streamChat` 透传 `signal`（`chat.ts:59`）；`cancelParticipant`（`:27`）→ `abort()`。停止循环（`index.tsx:163`）/ 移除参与者（`RoleChatSessionSidebar:38`）/ 行内停止按钮（`:106`）均调用——**在途 SSE 真正可中断**。
- **子问题 3（历史陈旧）✅ 干净修复**：`respondParticipant`（`:94`）每次实时 `store()` 读 `roleChatMessages`，纯函数 `buildParticipantMessages` 派生该参与者视角，成稿 append 回单一数据源。循环中各参与者**实时互见彼此新发言**，"多角色交流"核心价值恢复。
- **子问题 2（卸载清理）❌ 未落地**：`role-chat/index.tsx` 顶部仅 `import { useState, useRef }`，**全无 useEffect 卸载钩子**；`runAgentLoop`（`:127`）仍用布尔 `abortRef`。AppLayout 用 `<Outlet/>`（`:241`），切走 `/role-chat` 路由即卸载 `RoleChatPage`，而无人置 `abortRef.current=true` → 循环后台续跑、持续烧 token、用户失控（停止按钮已卸载）。HANDOFF 称"切走即 cancelParticipant"仅对**切换 session**成立（同组件条件渲染、不卸载，本就是设计意图），对**切走整个路由**不成立。严重性较原描述降低（改写全局 store，无 setState-on-unmounted；count/duration 模式终会自然停），但 token 浪费 + 失控属实。转 **C-2**。

### 6.3　monopoly 新模块 ☑ 质量优秀（无整改项）
原报告未覆盖。复审结论：**全项目工程纪律的正面样板**，与 role-chat 形成对照。
- **纯 reducer**（`engine.ts`）：随机源 `rollDice`（`:54`）独立于 reducer、dice 经 action 传入；reducer 内全部不可变拷贝、无 `Date.now`/`Math.random`/I/O。`as number` 等仅是 `DecisionRequest.context` 局部窄化，非裸 any。
- **AI 自动循环用正确的 effect-driven 模式**（`index.tsx:42-47`）：`useEffect(()=>{const t=setTimeout(()=>dispatch(aiNextAction(state)),AI_DELAY); return ()=>clearTimeout(t)},[state])`——**卸载有 cleanup ✓、依赖 `[state]` 每轮读最新无闭包陈旧 ✓**。B-2 的三个问题在此一个都没有。
- **Three.js 资源完整释放**（`Board3D.tsx:152-165`）：cancelAnimationFrame + removeEventListener + controls/renderer.dispose + 遍历 geometry/material.dispose + 移除 DOM 节点；`stateRef` + 单挂载 effect 避免重建场景。
- **零裸 any、无胖文件**（13 文件最大 engine.ts 301 行）。
- **唯一前瞻提示（低）**：`ai.ts` LLM 挂载点当前为规则式（未接 LLM）。一旦 P5 把 `aiDecide` 换成异步 LLM 调用，`setTimeout(()=>dispatch())` 的同步模式将不够——届时需引入异步 action + AbortController，并复核卸载语义（勿重蹈 role-chat B-2）。

### 6.4　🔴 新发现 C-1（中高）：roleChatAutoConfig 持久化丢失（确定性 bug）
role-chat 重构新增"配置项" `roleChatAutoConfig`（自动循环参数）。`bootstrap.ts:118-119` **读取**它（从 settings.json 恢复、缺键补默认），但 `persistence.ts` 的 `settingsPayload`（`:158-179`，20 键）**不含它**、设置订阅比较（`:305-324`）也不含它，后端 `settings.ts` 无特殊处理。→ **用户在「循环设置」里调整的 count/duration/cooldown 等参数永远写不回磁盘，重启即丢失**。这是 **B-3（承接 audit-01 P0-1）预言的"手写脏检查＝变更放大器，新增字段易漏比较致改了不落库"的真实兑现**——理论风险已成 user-facing bug。治标（C-1）：把 `roleChatAutoConfig` 加进 `settingsPayload` + 两处订阅比较；**根治**仍是 B-3 的声明式化（payload 键集驱动比较），否则此类漏写会反复发生。

### 6.5　新功能测试覆盖缺口
55 例全绿守护的是**重构产物**（cleanScheduler/persistence/dequeue/circuitBreaker/node-test 等 characterization 网）。本轮两个新模块**零测试**：`engine.ts`（纯 reducer）与 `roleChatEngine.ts` 的 `buildParticipantMessages`（纯函数派生）都是**单测的理想目标**（输入→输出确定、无 IO），却未补。延续 audit-02 主题"质量纪律需向新功能扩面"——建议新增纯逻辑必带单测。

### 6.6　B-3~B-11 回填一览
除 **B-7 因 chat.ts 删除而作废**外，B-3~B-6、B-8~B-11 **并行开发期间均未触及**（功能线未碰重构债，合理）。逐项证据见第 5 节追踪表状态列。

### 6.7　健康度复评

| 维度 | audit-02 | 复审增量 | 说明 |
|---|:--:|:--:|---|
| 架构与目录结构 | 7.5 | → 7.5 | monopoly 逻辑/渲染彻底分离、role-chat 引擎抽离，结构延续良好；三胖文件未减 |
| 模块拆分与解耦 | 6.5 | → 6.5 | role-chat 695→拆多文件（index 301 / 引擎 185 / 视图分离）正向；settings 1428 / useInferenceSession 780 / creation 731 仍在 |
| 代码质量与可维护性 | 6.5 | ↘ 6.0~6.5 | B-1 真修复 + monopoly 高质量（＋）；B-2 子问题2残留 + **C-1 确定性持久化 bug** + 新模块零测试（−）→ 净持平偏下 |
| 技术栈最佳实践 | 7.5 | ↗ 7.5~8.0 | monopoly 的 effect-driven 循环 / 纯 reducer / Three 资源管理为教科书级范例，正向拉升 |

**复审总评**：并行开发**真实修复了最危险的 B-1**（方案更彻底），**B-2 的功能性核心（流可中断 + 历史不陈旧）也已修复**——两个 P0 的 user-facing 危害基本解除。monopoly 模块证明团队**有能力写出高纪律代码**。但本轮也暴露两件事：① role-chat 重构在生命周期收尾上仍留缺口（C-2 卸载）；② **B-3 的债没还，立刻被 C-1 兑现成真 bug**——印证"声明式化持久化"不是洁癖而是止血。结论：**修复方向正确，收尾纪律与重构债清偿需补课**；monopoly 的工程范式值得反哺到 role-chat 与后续新功能。下一步优先级：**C-1（确定性数据丢失，单文件低风险）> C-2（循环失控）> B-3 根治 > 其余 B 项**。

---

## 7. 🔧 修复回填（2026-06-28 第三次走查·修复）

> 本节记录按第 6.7 节优先级顺序对复审遗留项的修复。**Claude 不执行 git**，下列改动待用户提交（追踪表「完成 commit」暂记"待提交"）。

### 7.0　范围与验证
- **修复 8 项**：C-1 / C-2 / B-3 / B-4 / B-6 / B-9 / B-10 / B-11（复审 6.7 优先级链全部 + 低优先级 B 项）。
- **未做 2 项**：B-5（useInferenceSession 抽 useCompareSession）/ B-8（creation.ts 拆三文件）——均为第三梯队"需先出设计稿"的结构性重构，留待专门设计。
- **回归基线**：后端 `tsc --noEmit` **0** + 前端 `build`（tsc -b + vite）**成功** + `vitest` **11 文件 55 例全绿** + 改动文件 `eslint` **0 错误**。

### 7.1　C-1 ☑ roleChatAutoConfig 持久化（确定性数据丢失止血）
`persistence.ts` 的 `settingsPayload` 补 `roleChatAutoConfig`；订阅比较侧不再手工加——交由 B-3 声明式化自动覆盖（payload 在则比较在）。独立验证后端 `/api/settings` 为 `writeSettings({...existing,...body})` 的 merge 透传（不过滤字段），确认"payload 含 → 落库 → bootstrap 读回"闭环成立。characterization 测试 `persistence.test.ts` 锁定的键集期望同步 20→21 键。

### 7.2　C-2 ☑ role-chat 卸载清理
`role-chat/index.tsx` 补卸载 `useEffect(()=>()=>{abortRef.current=true; getState().roleChatParticipants.forEach(p=>cancelParticipant(p.id))},[])`。读 store 最新参与者而非闭包快照；切走 `/role-chat` 路由即停自动循环 while + 中止全部在途 SSE 流，根除"后台续跑烧 token、停止按钮已卸载无法干预"。

### 7.3　B-3 ☑ 持久化脏检查声明式化（C-1 根治）
`registerPersisters` 内以 `BUSINESS_KEYS`/`SETTINGS_KEYS = Object.keys(payload(getState())) as (keyof AppState)[]` 求键集（**函数内**求值，规避文件头所述模块顶层循环依赖），两处订阅的手写 12/21 项 `===` 链改为 `KEYS.every(k=>s[k]===prev[k])`。此后"加设置字段只改 payload 一处，脏检查自动跟随"——C-1 这类"漏写比较致改了不落库"从机制上杜绝，复审 6.4 的"理论风险→真 bug"闭环就此封死。

### 7.4　B-4 ☑ settings 手写 SSE → parseSSE
`startRealTest` 的 `getReader+TextDecoder+split('\n\n')` 手写帧解析换 `for await (const {event,data} of parseSSE(res.body))`，按 event 分发：`delta` 累积、`done` 用全文覆盖。取证后端 `/api/llm/clean` 的 done 事件携带 `{text: full}`（完整文本）——原手写对 done 也执行 `acc += text` 会令测试结果显示两遍，本次按 event 正确分发一并理顺。

### 7.5　B-6 ☑ 类型注解订正
`bootstrap.ts` 局部 inline 类型注解 `batchSize`→`batchChars`，对齐 `types.ts:182` 真实字段（原透传不致运行时 bug，仅误导读者）。

### 7.6　B-9 ☑ 三弹窗卸载清理
`CardEditorModal`/`ImageBatchModal`/`SceneSettingModal` 各加 `useEffect(()=>()=>acRef.current?.abort(),[])`，补齐"父组件非 onCancel 路径卸载（切路由）"时在途流的中止。CardEditorModal 文件头"无 useEffect"注记同步订正。

### 7.7　B-10 ☑ import 收口
`SceneSettingModal` 与 `roleChatEngine` 的 `streamChat`（值）与 `ChatMessage`（类型）改经 `services/api` re-export，不再直 import `services/real/chat`，维持"业务调用经服务层、mock/real 可整层切换"。

### 7.8　B-11 ☑ sseHelper ACAO 对齐
利用 Fastify `reply.request` 在 `hijackSSE` 内读 `headers.origin`，回显白名单内 Origin（`localhost:5173`/`127.0.0.1:5173`/file:// 的 `null`），替代硬编码 `localhost:5173`。**单文件零调用点改动**——绕过了"creation.ts 的 `streamChat` helper 不持有 req"的扩散问题。原硬编码在改端口/非 Electron 环境会令浏览器拒收 SSE，现与主 CORS 逻辑一致。

### 7.9　遗留（B-5 / B-8）
两项均为第三梯队结构性重构，审计原定"需先出设计稿"，本轮未动：
- **B-5**：`useInferenceSession`（780 行）抽 `useCompareSession` + side 参数化消重 + 对比 acRef 卸载清理。
- **B-8**：`creation.ts`（731 行）按领域拆 `creation.origin`/`creation.generate`/`creation.m2` 三文件。

建议各自先出设计稿再实施。另复审 6.5 指出的新模块（monopoly engine / roleChatEngine `buildParticipantMessages` 纯函数）单测缺口仍在，可一并纳入后续。

### 7.10　修复后健康度
本轮修复对四维的增量：**代码质量与可维护性** 6.0~6.5 → **6.5~7.0**（C-1 确定性 bug 消除、B-2 卸载缺口补齐、持久化脏检查声明式化去除"变更放大器"、settings 显示 bug 理顺）；**架构与目录结构** 维持 7.5（import 收口、ACAO 对齐属局部规整）。重构债（B-3）已偿还核心一笔，剩余胖文件（B-5/B-8）转入待设计。**两个 P0 的全部子问题（B-1/B-2）与全部确定性问题（C-1）现已彻底关闭**。
