/**
 * 节点熔断器 —— 已迁移至 `packages/node-pool/circuitBreaker.ts`。
 *
 * 本文件保留 re-export，供现有调用方零改动继续使用。
 * 新代码建议直接从 `@/packages/node-pool/circuitBreaker` 导入。
 */
export { NodeCircuitBreaker } from '../../packages/node-pool/circuitBreaker'
