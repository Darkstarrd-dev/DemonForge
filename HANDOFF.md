# HANDOFF.md — 项目进展与任务交接

> **用途**：用户在两个办公场所之间通过 git 手动同步本项目。本文档是任一场所
> 拉取代码后**恢复工作的唯一入口**——新会话从这里了解进展、接续任务。
> **约定**：每完成一项任务即更新本文档（勾选 checklist、更新快照与交接备注），
> 之后由用户手动执行 git 同步。

## 快速恢复（新会话从这里开始）

1. **阅读顺序**：`CLAUDE.md`（工程约束与已确认决策）→ 本文档（进展与下一步）→
   `DESIGN.md`（总体设计）→ 当前任务涉及的 `docs/*.md`。
2. **当前阶段**：需求设计阶段，**尚未编码**——仅读写配置与设计文档，以沟通推演为主。
3. **当前任务焦点**：逐项处理 DESIGN.md §7 待讨论问题（1 已完成，正在等待用户填充
   raw 特征样本；接下来是问题 2–5）。

## 项目状态快照

- **最后更新**：2026-06-12
- **阶段**：设计（编码未开始，工程内无任何代码文件）
- **摘要**：总体设计 v0.1 完成（五模块 M1–M5、两库分离数据模型、技术架构、P0–P5 规划）；
  M1 已有详细设计（抽象自单页应用原型）；raw 特征样本模板已建待填。

## Checklist

### 已完成

- [x] 项目初始化：`CLAUDE.md`（阶段约束、工作方式、已确认决策）
- [x] `DESIGN.md` 总体设计 v0.1：五模块设计、素材库/作品库分离数据模型、技术架构、P0–P5 阶段规划
- [x] 关键决策确认：Node.js + React + Vite、SQLite（含向量检索）、Provider 抽象（本地+云端）、两库分离、按依赖链 M1→M5 开发
- [x] 待讨论问题 1：分析原型 `10_novel_cleaner.html` → 产出 `docs/M1_text_cleaning.md`（M1 详细设计，含原型缺陷修复清单）；建立 `docs/M1_raw_features.md` 模板
- [x] 建立本交接文档

### 进行中 / 等待用户

- [ ] **用户填充 `docs/M1_raw_features.md`**（粘贴真实 raw 文件片段：标题格式、广告样例、乱码样例等）

### 待办（设计阶段）

- [ ] 待讨论问题 2：M3 角色语言风格约束方式（卡片维护"风格描述 + 台词例句"是否足够）
- [ ] 待讨论问题 3：一致性检查触发时机（定稿自动 / 手动）
- [ ] 待讨论问题 4：静态/动态设定分离建议是否采纳
- [ ] 待讨论问题 5：后端框架 Fastify vs Express（默认 Fastify）
- [ ] 基于已填充的 raw 特征细化 M1 切分与清理规则
- [ ] M2 设定提取详细设计（实体合并策略、卡片字段、增量抽取）
- [ ] M3 单角色推演详细设计（上下文组装器、排练界面交互）
- [ ] M4 章节生成详细设计（片段硬约束 prompt 方案）
- [ ] M5 管理与一致性详细设计（状态时间线、检查规则）
- [ ] 设计定稿评审 → 解除"不编码"约束，进入实现阶段

### 待办（实现阶段，设计定稿后启动）

- [ ] P0 基础设施：项目骨架、SQLite 初始化、Provider 抽象层、配置界面
- [ ] P1 = M1 文本预处理
- [ ] P2 = M2 设定提取
- [ ] P3 = M3 单角色推演
- [ ] P4 = M4 章节生成
- [ ] P5 = M5 管理与一致性

## 交接备注（最近一次会话）

- **日期**：2026-06-12
- **本次完成**：项目初始化两轮沟通（定位/形态/技术栈/数据模型/开发顺序均已拍板）；
  原型应用分析并产出 M1 详细设计；raw 特征模板；本交接文档。
- **下一步建议**：① 用户填充 `docs/M1_raw_features.md`；② 回复待讨论问题 2–5
  （见 `DESIGN.md` §7），可逐条进行。
- **阻塞项**：无（等待用户输入）。

## 更新本文档的约定

1. **完成任务**：勾选对应 checklist 项；产生新任务则新增条目。
2. **会话结束前**：刷新"项目状态快照"与"交接备注"（日期、完成内容、下一步、阻塞项）。
3. **新决策产生**：同步写入 `CLAUDE.md` 已确认决策表与 `DESIGN.md` 对应章节，本文档只记进展不重复细节。
